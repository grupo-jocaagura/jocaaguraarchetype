part of 'package:jocaaguraarchetype/jocaaguraarchetype.dart';

/// Global UI constants used across the archetype.
///
/// Keep values minimal and platform-agnostic. Prefer injecting configurable
/// values via AppConfig for environment-specific behavior.
///
/// ### Example
/// ```dart
/// import 'package:jocaaguraarchetype/consts/app_constants.dart';
///
/// SizedBox(height: AppConstants.screenPadding);
/// ```

const double kAppBarHeight = 60.0;
